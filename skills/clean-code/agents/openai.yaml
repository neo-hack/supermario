interface:
  display_name: "Clean Code"
  short_description: "Successive simplification passes over live code"
  default_prompt: "Use $clean-code to simplify the target code with successive Clean Code passes."
