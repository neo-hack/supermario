interface:
  display_name: "Mockup"
  short_description: "Frame screenshots for product mockups"
  default_prompt: "Use $mockup to create a polished framed PNG mockup from this image, URL, or local HTML file."
