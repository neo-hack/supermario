interface:
  display_name: "Fire"
  short_description: "Dead-code cleanup and duplicate reports"
  default_prompt: "Use $fire to dispatch explorer agents, analyze dead code evidence, report duplicated code, and safely remove proven unused code."
