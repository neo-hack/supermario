---
"supermario": minor
---

codemermaid skill: replace Mermaid.js CDN with beautiful-mermaid browser bundle for diagram rendering. Adds `mermaid-bridge.js` with Raycast dark theme, `ARCHITECTURE_DIAGRAM` slot on index page, and `.mermaid-error` error styling.
