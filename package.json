{
  "name": "supermario",
  "version": "0.1.0",
  "private": true,
  "description": "Agent skills for documentation, code annotation, visual mockups, and development workflows.",
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "build:mermaid": "bash scripts/build-mermaid.sh",
    "plugin:marketplace:add": "claude plugin marketplace add ./ --scope project",
    "plugin:install": "claude plugin install supermario@supermario-marketplace --scope project",
    "plugin:validate": "claude plugin validate .claude-plugin/marketplace.json && claude plugin validate plugins/supermario/.claude-plugin/plugin.json",
    "test": "node --test tests/**/*.test.js tests/**/*.test.mjs",
    "changeset": "changeset",
    "changeset:status": "changeset status"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "js-yaml": "^4.1.1"
  }
}
